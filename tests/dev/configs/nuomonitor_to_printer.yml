---
NUOCA_LOGFILE: /tmp/nuoca_nuomonitor_to_printer.log

INPUT_PLUGINS:
- NuoMon:
    description : Collection from internal nuomonitor tool
    broker: localhost
    domain_username: domain
    domain_password: bird
    domain_metrics_host: localhost

OUTPUT_PLUGINS:
- Printer:

